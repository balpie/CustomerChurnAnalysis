## training
Utilizzando una struttura a moduli piuttosto che script, per effettuare il 
training dei modelli ora bisogna lanciare il comando
`python -m training.<modello>.<script>`
per esempio
`python -m training.sgdc.sgdc_log_loss`
